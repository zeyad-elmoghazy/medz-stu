node_modules
.next
out
build
dist
.env
.env.local
.env*.local
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts

# Reference-page images (book digitization pipeline) — large binary
# assets, not source. Covers both the intended scripts/import/
# reference-pages/<book>/ layout and the flat scripts/import/*.png
# pattern one batch actually landed in.
scripts/import/reference-pages/
scripts/import/*.png

# Auto-generated by Next.js 16's `agentRules` feature on the first
# `next dev`/`next build` in a repo that doesn't have them yet — pure
# framework boilerplate, not authored content. Set `agentRules: false`
# in next.config.js instead if you'd rather Next stop writing these.
AGENTS.md
CLAUDE.md
